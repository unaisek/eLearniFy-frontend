import { NoEmptySpacesDirective } from './no-empty-spaces.directive';

describe('NoEmptySpacesDirective', () => {
  it('should create an instance', () => {
    const directive = new NoEmptySpacesDirective();
    expect(directive).toBeTruthy();
  });
});
